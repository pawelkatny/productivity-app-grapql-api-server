type AuthUser {
  name: String!
  token: String!
}

input RegisterUserInput {
  name: String!
  email: String!
  password: String!
}

type Mutation {
  registerUser(input: RegisterUserInput!): AuthUser!
}

type Query {
  user: String
}
